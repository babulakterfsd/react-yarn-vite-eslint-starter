import React from 'react';

function MainLayout() {
    return (
        <div>
            <div>
                {/* this layout will be used for showing the main site features. basically, if user is logged in, he / she can see this layout. but this is not mandatory too */}
            </div>
        </div>
    );
}

export default MainLayout;
